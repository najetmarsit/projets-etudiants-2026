import { Component } from '@angular/core';

/** Composant minimal pour les routes dont la navigation est entièrement gérée par un garde (UrlTree). */
@Component({
  selector: 'app-empty-redirect',
  standalone: true,
  template: '',
})
export class EmptyRedirectComponent {}
