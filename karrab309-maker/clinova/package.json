{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "start": "concurrently -k -n api,front -c green,cyan \"php artisan serve --host=127.0.0.1 --port=8000\" \"npm start --prefix angular-app\"",
        "start:api": "php artisan serve --host=127.0.0.1 --port=8000",
        "start:front": "npm start --prefix angular-app"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "concurrently": "^9.0.0",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
